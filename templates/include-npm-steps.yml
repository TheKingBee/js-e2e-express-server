steps:
  - task: UseNode@1
    inputs:
      version: '20.x'
    displayName: 'Installing NodeJS 20.x LTS'
  - script: |
      npm install
      npm run build
      npm run test
    displayName: "npm install, build, and test"
